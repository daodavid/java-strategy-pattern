package strategyPattern;

public interface Strategy {
  /*
   * the stategy pattern represents,the way for modifying 
   * the behaviour of class at the runtime
   * 
   */
	public int doOperation(int a,int b);
}
